# React Universal Renderer Example

An example using [React Universal Renderer](https://github.com/acorcutt/react-universal-renderer) to render an async request on the server before initializing the client.